{
  "name": "isaaccss",
  "version": "0.0.0",
  "description": "A CSS class DSL like inline styles",
  "license": "WTFPL",
  "bin": "lib/cli.js",
  "type": "module",
  "main": "lib/index.node.js",
  "types": "lib/index.node.d.ts",
  "browser": "lib/bundle.browser.js",
  "prettier": {
    "printWidth": 140,
    "arrowParens": "avoid"
  },
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('lib',{recursive:true,force:true})\"",
    "build": "npm run clean && esbuild src/index.browser.ts --bundle --minify --format=esm --outfile=lib/bundle.browser.js && tsc -p .",
    "start": "npm run clean && esbuild src/index.browser.ts --bundle --minify --format=esm --outfile=lib/bundle.browser.js --servedir=."
  },
  "devDependencies": {
    "@types/glob": "^7.2.0",
    "@types/node": "^18.7.11",
    "esbuild": "^0.15.5",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@babel/parser": "^7.18.13",
    "@babel/types": "^7.18.13",
    "css.escape": "^1.5.1",
    "glob": "^8.0.3",
    "htmlparser2": "^8.0.1",
    "known-css-properties": "^0.25.0"
  }
}
